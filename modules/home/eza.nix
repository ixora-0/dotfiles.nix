{
  programs.eza = {
    enable = true;
    enableZshIntegration = true;  # automatically add aliases
                                  # should be defaulted to true already
    icons = "auto";
  };
}
